{
  "name": "prisma",
  "version": "1.0.0",
  "description": "Prisma database setup for the Codegen Web App",
  "main": "index.js",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "studio": "prisma studio",
    "test": "echo \"No tests specified for prisma yet\" && exit 0",
    "build": "echo \"No build step required for prisma workspace\" && exit 0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.6.0"
  }
}
